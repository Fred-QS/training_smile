{% extends 'base.html.twig' %}

{% block title %}Hello MovieController!{% endblock %}

{% block body %}
    <ul>
    {% for movie in movies %}
        <li>
            <a href="{{ path('app_movie', {'id': movie.id}) }}">
                {{ movie }}
            </a>
        </li>
    {% endfor %}
    </ul>
{% endblock %}
